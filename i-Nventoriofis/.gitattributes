# Shell scripts must keep LF. A .command file checked out with CRLF fails on
# macOS with "bad interpreter: /bin/bash^M", which is a confusing way to
# discover a line-ending problem.
*.command text eol=lf
*.sh      text eol=lf
